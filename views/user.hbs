<main class="container user-main" id="userHome">
    <div class="container blog-top">

        <div class="jumbotron blog p-3 p-md-5 text-white rounded bg-dark">
        <div class="col-md-6 px-0">
          <h1 class="display-4 font-italic">You can now book tickets for your friends...</h1>
          <p class="lead my-3">Book my Events helps you to easily book tickets and need not to worry about. Because you have to pay at the event. Book tickets for your friends and Dont forget to bring your Id with you.</p>
          <p class="lead mb-0"><a href="#" class="text-white font-weight-bold">Don't want to buy no problem.Please take a look at the events...</a></p>
        </div>
        <div class="col-md-6 header-image">

        </div>
      </div>

    </div>
    <div class="row">
        {{#each form}}

        <div class="col-lg-2 col-md-0 col-sm-0"></div>
        <div class=" mb-r col-lg-8 col-md-12 col-sm-12">
            <div class="card  text-center">
                <div class="card-header">
                    <h5>{{title}}</h5>
                </div>
                <div class="card-body">
                    <p class="card-text">{{date}} | {{time}} | {{location}} </p>
                    <h6 class="card-title">{{description}}</h6>
                    <p class="card-text">{{seats}} Seats | {{ageRestriction}} | {{genderRestriction}} </p>
                    <form id="idregister" method="GET" action="/register/{{_id}}">
                        <div class="form-group ">
                            <div>
                                <button type="submit" class="btn btn-primary">Register Now</button>
                                <a class="btn btn-primary">Comments</a>
                            </div>

                        </div>
                    </form>

                </div>
                <div class="card-footer text-muted">
                    <h5>${{cost}}</h5>
                </div>
            </div>
        </div>
        <div class="col-lg-2 col-md-0"></div>

        {{/each}}
    </div>
</main>